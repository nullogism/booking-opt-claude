# ┌─────────────────────────────────────────────────────────────────────────────
# │ 1) Operating System files
# └─────────────────────────────────────────────────────────────────────────────

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Linux swap files
*~

# ┌─────────────────────────────────────────────────────────────────────────────
# │ 2) Editor/IDE files
# └─────────────────────────────────────────────────────────────────────────────

# VS Code
.vscode/

# IntelliJ / WebStorm / PyCharm
.idea/
*.iml

# Sublime Text
*.sublime-project
*.sublime-workspace

# Vim swap files
*.swp

# ┌─────────────────────────────────────────────────────────────────────────────
# │ 3) Python artifacts (FastAPI back‑end)
# └─────────────────────────────────────────────────────────────────────────────

# Byte-compiled / optimized
__pycache__/
*.py[cod]
*$py.class

# Virtual environment directories (if you ever name them differently)
.venv/
venv/
env/
env.bak/

# Dist / build outputs (if you ever package wheels, etc.)
build/
dist/
*.egg-info/
*.egg
pip-wheel-metadata/
.eggs/

# Caches / coverage
.pytest_cache/
.mypy_cache/
__pypackages__/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Logs
*.log

# Database files (if you have a local SQLite during development)
*.sqlite3

# Environment variables / secret files (FastAPI often uses these)
.env
.env.*         # e.g. .env.local, .env.production, etc.
secrets/       # if you keep a secrets folder with keys
*.pem
*.key
key.json       # if you place service accounts or GCP keys here by accident

# ┌─────────────────────────────────────────────────────────────────────────────
# │ 4) Node/JavaScript (Front‑end) – only if/when you add a package.json, npm, etc.
# └─────────────────────────────────────────────────────────────────────────────

# Dependency directories
node_modules/

# Log files generated by npm/yarn
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# lockfiles (uncomment if you do NOT want to commit lockfiles; usually you do commit these)
# package-lock.json
# yarn.lock

# Front‑end build output (if you ever run a bundler like Webpack, Rollup, etc.)
dist/
build/

# ┌─────────────────────────────────────────────────────────────────────────────
# │ 5) Docker-related (optional / only if you ever generate local tarballs, overrides)
# └─────────────────────────────────────────────────────────────────────────────

# Ignore local Docker “docker-compose.override.yml” if you use one for local dev
docker-compose.override.yml

# If you ever create *.tgz archives of your images or Helm charts
*.tgz

# ┌─────────────────────────────────────────────────────────────────────────────
# │ 6) Kubernetes manifests & secrets (optional additions)
# └─────────────────────────────────────────────────────────────────────────────

# If you keep generated secrets or k8s YAML with unencrypted credentials:
*.secret.yaml
*.kubeconfig
*.kubeconfig.*   # e.g. kubeconfig.dev, etc.

# If you generate local backup copies (e.g., vim~) of your YAML files
*.yaml~

# ┌─────────────────────────────────────────────────────────────────────────────
# │ 7) Miscellaneous / temporary
# └─────────────────────────────────────────────────────────────────────────────

# OS or editor temp files
*.bak
*.orig

# if you accidentally create zip/tar archives for local debugging
*.zip
*.tar.gz

# any other throwaway directory (e.g., “tmp/”)
tmp/

